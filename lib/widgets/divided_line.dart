import 'package:flutter/material.dart';

class DividedLine extends StatelessWidget {
  const DividedLine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Colors.black12,
      thickness: 1,
    );
  }
}
